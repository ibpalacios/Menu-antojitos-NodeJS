# Menu-antojitos-NodeJS

## Comando iniciales

Ejecutar `npm i` para instalar dependencias.

Ejecutar `node src/server.js` para correr el servidor, se ejecutara el servidor y la base de datos que esta alojada en atlas.
